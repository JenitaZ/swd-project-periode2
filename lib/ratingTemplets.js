const rating = document

rating.array.forEach((score) => {
    const selectedRating = rating.innerHTML;
    const ratingItem = parseInt(ratingRating, 10);
    const scoreClass =
  
    rating.classList.add(scoreClass);
  });
  